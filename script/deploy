#!/bin/sh
ssh ubuntu@13.124.150.142 <<EOF
  sudo docker pull hch950627/chang:2.0
  sudo docker rm `docker ps -a -q`
  sudo docker run -p 3000:3000 --name antsori -it -d hch950627/chang:2.0
  exit
EOF
